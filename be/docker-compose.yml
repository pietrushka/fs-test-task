version: "3.1"

services:
  db:
    container_name: mongoDB
    image: mongo:latest
    restart: always
    volumes:
      - ./productData:/data/db
    environment:
      - MONGO_INITDB_DATABASE=Products
    ports:
      - 27017:27017